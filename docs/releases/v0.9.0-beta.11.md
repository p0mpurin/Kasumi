Clean cover art in the library.

- Fixed: after playing, some covers in the library could show up broken, with blocks of the picture missing. Leftover data from the video buffers could be written over a cover as it loaded; Kasumi now clears it out first
- Covers from a download that was cut off are no longer saved to the SD card, and a cover that failed to load is tried again after a few seconds
- The cover cache is cleared once on the first start of this version, so any broken cover already saved is downloaded again

Update from Settings > Updates, or install with FBI (Remote Install > Scan QR Code). Your login, library and settings are kept.
