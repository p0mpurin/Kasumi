Clearer reconnects when a stream is interrupted.

- Fixed: Kasumi could think it had been away for minutes after a short HOME visit, because it compared against an older pause, and reconnect when it didn't need to. Only a pause during the current stream counts now
- Fixed: when NVIDIA has already ended the session on its side, Kasumi no longer retries three times with "Invalid WebSocket upgrade HTTP=404". It says the session was ended and A starts the game again
- The diagnostic log now records what paused the app mid-game (sleep or HOME/system screen) and whether the lid read as closed, to track down unexpected interruptions
- Includes the beta.6 fix for the CIA freezing at stream start

Update from Settings > Updates, or install with FBI (Remote Install > Scan QR Code). Your login, library and settings are kept.
